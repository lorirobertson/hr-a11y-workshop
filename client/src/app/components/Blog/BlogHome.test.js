import React from 'react';
import { mount, shallow } from 'enzyme';
import { expect } from 'chai';

//import BlogHome from './BlogHome';

// const wrapper = shallow(<BlogHome match={{isExact: true, path: '', url: ''}}/>);

// describe("<BlogHome />", function() {
//     it("has the correct ID.", function() {
//         expect(wrapper).to.have.property('id').to.equal('blog-container');
//     });
// });